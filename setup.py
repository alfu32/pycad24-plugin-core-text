from setuptools import setup, find_packages

setup(
    name='pycad24-plugin-core-line',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
    ],
    entry_points={
        # Add entry points here if necessary
    },
)
